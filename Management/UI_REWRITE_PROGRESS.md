# Management UI/Design-System Rewrite — Progress

Tracks execution of the plan approved 2026-08-22. Full plan (context, decisions, rationale) lives at the top of this session's plan file; this doc is the checklist + resumption point for future sessions.

**Branch:** `feat/management-ui-design-system-rewrite` (base: `microservices`)
**Execution mode:** autonomous, step by step, one commit per completed unit. Read this file first at the start of every session before doing anything else.

## Decisions locked in (do not re-litigate)

- Foundation: shadcn/ui component patterns on **Base UI** primitives, on **Tailwind v4**.
- Rollout: incremental, feature-folder by feature-folder — no big-bang rewrite.
- TypeScript: migrated alongside, file-by-file as each folder is touched (`allowJs` during transition).
- Cleanup included: remove dead `@mui/*`/`@emotion/*`/`@headlessui/react`; consolidate the two duplicate `StatCard` components.
- Forms: adopt `react-hook-form` + `@hookform/resolvers` (zod).
- Toast: **keep `react-hot-toast` for now** — revisit after primitive migration lands (Phase 6), not before.
- Copy/label changes allowed during redesign; update the matching Playwright spec in the same commit, and report the full before/after text to the user when it happens.
- Color palette and font pairing are **not yet decided** — make that call in Phase 1 via the `frontend-design` skill, present concrete options to the user, don't default silently.

## Phase 0 — Tooling & Infrastructure

- [x] 0.0 Create this progress file, commit first
- [x] 0.1 Remove dead deps (`@mui/material`, `@mui/icons-material`, `@mui/lab`, `@mui/x-date-pickers`, `@emotion/react`, `@emotion/styled`, `@headlessui/react`), `npm install`, re-grep to confirm
- [x] 0.2 TypeScript scaffolding: `tsconfig.json` (`allowJs`, `checkJs: false`, `strict: false`, aliases mirroring `@`/`@utils`), `tsconfig.node.json`, rename `vite.config.js`/`vitest.config.js` → `.ts`, extend `eslint.config.js` with a `**/*.{ts,tsx}` block at the same `warn` posture
- [x] 0.2b Third-party type-compat audit (lodash, js-cookie, file-saver, qrcode, json2csv, leaflet/react-leaflet, react-datepicker, react-phone-number-input, react-select, sweetalert2, i18next/react-i18next, zod v4) — add ambient `.d.ts` shims where needed
- [x] 0.3 Tailwind v4 upgrade (CSS-first `@theme`, PostCSS setup)
- [x] 0.4 shadcn/Base UI init (`components.json`, `src/lib/utils.ts`, `src/components/ui/` scaffold), add `@base-ui/react` + `clsx`/`tailwind-merge`/`class-variance-authority`
- [x] 0.5 Document the strictness ramp (non-strict now → tighten after Phase 2 → full `strict: true` after Phase 5)

**Gate before moving to Phase 1:** `npm run dev`, `npm test`, `npm run test:e2e`, `npm run lint`, `npm run build` all green, app pixel-identical to before.

## Phase 1 — Design Token Foundation

- [x] 1.1 Color system — real semantic palette (primary/neutral/success/warning/danger/info), CSS variables for light+dark.
- [x] 1.2 Real dark mode: CSS var pairs (`:root`/`.dark`), rebuilt `ThemeContext.tsx` (persistence + `prefers-color-scheme` + `document.documentElement` class toggle)
- [x] 1.3 Typography — real font pairing + type scale.
- [x] 1.4 Spacing/radius/shadow scale (motion vocabulary deferred — see note below)
- [x] 1.5 Author `Management/DESIGN.md` (frontmatter + Overview/Colors/Typography/Layout/Elevation/Shapes/Components/Do's-Don'ts/Responsive/Iteration Guide/Known Gaps, per the `linear.app/DESIGN.md` reference structure) — passes `npx @google/design.md lint` with 0 errors
- [x] 1.6 Verify light/dark (automated test suite + build/CSS-output spot checks — see note below)

**Gate:** tokens finalized, `ThemeContext` v2 working, `DESIGN.md` accurate, no `features/*` changes yet.

## Phase 2 — Shared Primitive Library

- [x] 2.1 Core primitives: Button, Card, Dialog, Table, Badge, Input/Select/Textarea, Tabs, Popover
- [x] 2.2 `src/components/shared/StatCard.tsx` (superset props, semantic-token colors) — duplicates deleted when their feature folder migrates (Phase 4/5), not here
- [x] 2.3 Table primitive (sort/empty/loading conventions) — `src/components/shared/DataTable.tsx`
- [x] 2.4 `react-hook-form` + zod resolver wiring, shared `Form`/`FormField` wrapper — `src/components/ui/form.tsx` (binds RHF to shadcn's `Field`/`FieldLabel`/`FieldError`, which ship library-agnostic in this shadcn version)
- [x] 2.5 Migrate `Sidebar.jsx` → `.tsx` and `ProtectedRoute.jsx` → `.tsx` (both under continuous `rbac.spec.js` coverage — update the spec + report exact before/after text if labels change)

**Gate:** `rbac.spec.js`/`login.spec.js` green against the new shell.

## Phase 3 — Feature Migration, Wave 1

- [x] 3.1 `features/dashboard` (3 files)
- [x] 3.2 `features/career` (3 files)

## Phase 4 — Feature Migration, Wave 2

- [x] 4.1 `features/analytics` (31 files) — delete `features/analytics/components/Common/StatCard.jsx`, repoint to shared StatCard
- [x] 4.2 `features/shared` (8 files: FlightSelectionModal, HotelSelectionModal, FlightPreferenceCard) — functionally complete and live-verified; has known control-height/sizing-consistency debt found in the same review, deliberately deferred to Phase 6.6, not fixed here
- [ ] 4.3 `features/itinerary` (45 files)
- [ ] 4.4 `features/lead-management` (42 files) — **`npm run test:e2e` is a hard merge gate**; preserve or deliberately update (with full before/after report) the accessible names `lead-lifecycle.spec.js` asserts on

## Phase 5 — Feature Migration, Wave 3

- [ ] 5.1 `features/user-management` (51 files) — delete `features/user-management/components/Common/StatsCard.jsx`; verify `/users`/`/settings` access-denial still passes

## Phase 6 — Hardening & Closeout

- [ ] 6.1 Flip `strict: true`, fix resulting errors
- [ ] 6.2 Re-check type-shim audit, drop unnecessary ambient `.d.ts`
- [ ] 6.3 Sweep `src/pages/*` page shells not covered by a feature folder for leftover gradient/stock-palette usage
- [ ] 6.4 Full `test`/`test:e2e`/`lint`/`build` gate
- [ ] 6.5 Revisit `react-hot-toast` → shadcn/Base UI toast replacement decision (deferred item)
- [ ] 6.6 **Control-height & sizing-consistency retrofit** (added 2026-08-23, after `DESIGN.md`'s Control Sizing & Consistency rule was written) — sweep *every* phase completed before that rule existed (0 through whatever's landed by the time this item starts) for: (a) same-row height mismatches between controls, (b) same-role components (segmented toggles/filters etc.) sized differently across screens, (c) arbitrary sub-12px text. Seed list, so specific instances aren't lost by the time this runs: `FlightSelectionModal.tsx:514-528` (Tabs `h-8` next to `Select size="sm"`/`h-7` in one row), `FlightSelectionModal.tsx:530-590` and `HotelSelectionModal.tsx:398-448` (legacy `AirportAutocomplete`/`PassengerSelector`/`CountrySelect`, all ≈42-44px, sitting beside new `h-8` primitives in the same row), `Sidebar.tsx:248` (`text-[11px]` label), `DashboardContainer.tsx:250,254,258` (`text-[10px] sm:text-xs` stat captions). Deliberately scheduled *after* Phases 4.3/4.4/5, not interleaved with them — those phases are built under the new rule from the start and shouldn't need retrofitting; this item is only for debt that predates the rule.

## Last session

- **2026-08-22**: Plan approved. Created this progress file and the `feat/management-ui-design-system-rewrite` branch (base `microservices`). Completed 0.1: removed `@mui/*`/`@emotion/*`/`@headlessui/react` (confirmed 0 usages via grep before and after), reinstalled with `--legacy-peer-deps` (matches how the existing lockfile was already resolved — a pre-existing vite@8/`@vitejs/plugin-react`@4 peer-dep mismatch unrelated to this change, not fixed here as out of scope). Verified: `npm run build` ✓, `npm run lint` ✓ (146 warnings, 0 errors — matches pre-existing baseline), `npm test` ✓ (260/260). `npm run test:e2e` not run this commit (requires full local stack up; skipped as low-risk given confirmed zero usage of removed packages) — run it before the next phase that touches Sidebar/ProtectedRoute (Phase 2).
  Completed 0.2/0.2b/0.5: added `typescript` (pinned to `^6.0.3` — **note: `typescript@7.x` is now the npm-default "latest" but `typescript-eslint@8.67` does not support it yet**, hard-errors on import; stay on the 6.x line until typescript-eslint publishes TS7 support, don't blindly bump), `@types/react`/`@types/react-dom` pinned to `^18` (matching the installed React 18 runtime — npm's un-pinned install grabbed `@types/react@19` initially, which is wrong for this app and was corrected), `@types/node`, `typescript-eslint`. Added `tsconfig.json` (`allowJs`/`checkJs:false`/`strict:false`, `@`/`@utils` path aliases, no `baseUrl` — deprecated in TS6→7 with `moduleResolution:"bundler"`, `paths` alone works) and a standalone `tsconfig.node.json` for the two config files (not wired via TS project references — hit `composite`/`noEmit` conflicts for no real benefit, dropped the reference). Renamed `vite.config.js`→`.ts`, `vitest.config.js`→`.ts` (also switched its `defineConfig` import from `'vite'` to `'vitest/config'` to get the `test:` field typed). Extended `eslint.config.js` with a `**/*.{ts,tsx}` block using `typescript-eslint`'s non-type-checked `recommended` config at the same `warn` posture as the JS block. Added `npm run typecheck` script. Third-party audit (0.2b): every package needing types has real ones — installed `@types/{lodash,js-cookie,file-saver,qrcode,json2csv,leaflet}`; `react-leaflet`, `react-datepicker`, `qrcode.react`, `react-select`, `sweetalert2`, `i18next`, `react-i18next`, `zod` all ship their own; `@types/react-phone-number-input` turned out to be a deprecated stub (the package ships its own types) — installed then removed. **No ambient `.d.ts` shims were needed at all.** Verified: `npm run typecheck` ✓, `npm run lint` ✓ (146/0, unchanged), `npm run build` ✓, `npm test` ✓ (260/260).
  Completed 0.3: ran the official `npx @tailwindcss/upgrade` codemod (required `--force` — it does a repo-wide git-clean check and the *repo root* has pre-existing unrelated untracked files (`.idea/`, `secret/`, `test.md`) outside Management; verified `Management/` itself was clean first). It auto-migrated `tailwind.config.js`'s `theme.extend.colors` into a `@theme { --color-*: ... }` block at the top of `src/index.css`, switched `@tailwind base/components/utilities` to `@import 'tailwindcss'`, and — notably — auto-added a `@layer base` compatibility shim for v4's default-border-color change (v3→v4 changed the default border color from `gray-200` to `currentcolor`; the shim restores the old default so this stays a non-visual change). The tool's own `npm add tailwindcss@latest` step failed (same pre-existing vite8/`@vitejs/plugin-react`4 peer conflict as everywhere else in this repo) so I finished the dependency swap manually: installed `tailwindcss@^4` + `@tailwindcss/vite` with `--legacy-peer-deps`, added the `tailwindcss()` plugin to `vite.config.ts`, deleted `postcss.config.js` and `tailwind.config.js` (fully superseded — no `@config` directive was added, so nothing still references the JS file) and removed the now-unnecessary `autoprefixer` dependency (v4 handles vendor prefixing internally).
  **One known, accepted non-pixel-identical detail**: v4's default gradient color interpolation space changed (`bg-gradient-to-r` etc. now compile with `in oklab` interpolation instead of v3's implicit sRGB) — this is the standard, expected effect of the official upgrade path (not something the tool "fixes," since it's considered a rendering improvement) and affects the ~50 files using gradient utilities. Visually this is a subtle smoothing of gradient stops, not a broken/missing style — flagging it rather than silently claiming zero visual diff.
  Verified: `npm run build` ✓ (CSS output compiles, spot-checked `bg-gradient-to-r`/`rounded-2xl`/`shadow-xl`/`text-blue-600` are all present in the built CSS), `npm run lint` ✓ (146/0, unchanged), `npm run typecheck` ✓, `npm test` ✓ (260/260), `npm run dev` boots and serves 200 on :5174 (started and stopped manually, no console errors in the log).
  Completed 0.4 (**Phase 0 is now fully done**): initialized shadcn/ui on Base UI via `npx shadcn@latest init -t vite -b base -p nova`. Two hurdles, both solved:
  1. The CLI prompts interactively for a style **preset** even with `-y` — there's no non-interactive "blank/custom" option, so I picked `nova` (Lucide icons + Geist font) since it matches the already-installed `lucide-react` icon set (94 existing usages) rather than introducing a second icon library. The preset only seeds initial placeholder tokens; Phase 1 fully overrides them, so this choice is low-stakes.
  2. The CLI's internal `npm install` steps (both for its own dependencies and for anything installed later via `shadcn add`) kept failing on this repo's pre-existing peer-dependency conflicts — first the known vite8/`@vitejs/plugin-react`4 mismatch, then a new one: `@base-ui/react` has an optional peer on `date-fns@^4` while the app uses `date-fns@2.30.0` (used elsewhere in the app; not upgraded, out of scope). The CLI has no flag to pass `--legacy-peer-deps` through. Fixed for good by adding **`Management/.npmrc`** with `legacy-peer-deps=true` — this isn't a one-off workaround, it's needed for every future `npx shadcn add <component>` call in Phase 2+ too.
  Result: `components.json` (aliases match the plan: `ui`→`@/components/ui`, `utils`→`@/lib/utils`, plus a `hooks`→`@/hooks` alias the CLI adds by default), `src/lib/utils.ts` (the `cn()` helper), and a first real primitive `src/components/ui/button.tsx` (Base UI `Button` wrapped with `cva` variants) — confirms the whole toolchain (Base UI + cva + Tailwind v4 + our path aliases) works end to end. `src/index.css` gained `@import "tw-animate-css"`, `@import "shadcn/tailwind.css"` (the base color-token CSS variables — `--background`/`--foreground`/`--primary`/etc. — live in this imported package rather than inlined; Phase 1 overrides them), `@import "@fontsource-variable/geist"`, and a `@custom-variant dark (&:is(.dark *))` (exactly the mechanism Phase 1.2's real dark mode plan needs) — our existing color `@theme` block and all the hand-written CSS below it (mobile utilities, phone-input overrides) were left untouched. New deps: `@base-ui/react`, `@fontsource-variable/geist`, `class-variance-authority`, `clsx`, `tailwind-merge`, `tw-animate-css`, and `shadcn` itself (the CLI, kept as a regular dependency per the tool's own convention — harmless, never imported by app code).
  Verified: `npm run typecheck` ✓, `npm run build` ✓ (Geist font files are now bundled since the CSS `@import`s them globally, but nothing renders it yet — `body`'s explicit font-family rule still wins over `html`'s new `font-sans` class, so this is inert until Phase 1's real typography decision; flagging rather than silently shipping unused font weight), `npm run lint` ✓ (147/0 — one new expected warning on `button.tsx`, `react-refresh/only-export-components` for exporting both `Button` and `buttonVariants` from one file, which is upstream shadcn's own standard pattern, not a mistake), `npm test` ✓ (260/260).
  Next: **Phase 1 — Design Token Foundation.** This phase needs the user's actual input (color palette direction, font pairing) via the `frontend-design` skill before any tokens get finalized — don't pick silently.

- **2026-08-22 (same session, continued)**: **Phase 1 complete.** Presented 3 concrete design directions (Boarding Pass/travel-editorial, Ledger/private-banking, Mission Control/dark-industrial) via `AskUserQuestion` with real preview swatches — user didn't pick one outright, gave direct feedback instead: liked Mission Control's operational/mono-for-numbers philosophy, but wanted **both light and dark** (not dark-only) and explicitly **no warm/luxury tones** — "colours with directly operational... clean look." Built a working HTML preview artifact ("Signal Console" — cool neutrals, single functional teal accent, Archivo/Public Sans/JetBrains Mono, light+dark toggle, realistic CRM component previews) via the `artifact-design` skill and published it for review before writing any real code. User confirmed it's good and asked whether it's changeable later — answered yes (that's the point of token-first) and proceeded to implement for real:
  - **`src/index.css`**: deleted the old no-op v3-migrated `blue`/`cyan`/`indigo`/`sky`/`primary`/`brand` color block entirely (confirmed zero usages, same grep-verified finding as Phase 0.1 — raw `bg-blue-*` etc. classes in not-yet-migrated features keep working via Tailwind v4's own built-in palette regardless). Replaced the shadcn-scaffolded placeholder neutral OKLCH `:root`/`.dark` values with real Signal Console tokens (hex, not oklch — simpler, matches the reviewed artifact exactly): `primary` teal `#0f7f8c`/`#2dd4d9`, full `background`/`foreground`/`card`/`secondary`/`muted`/`accent`/`border`/`ring` ladder, plus **added** `success`/`warning` tokens (shadcn's default schema only ships `destructive`) mapped into the `@theme inline` block as `--color-success`/`--color-warning` so `bg-success` etc. utilities exist. Added a 5-hue `chart-1..5` categorical palette for `recharts` (teal + slate-blue + amber + muted violet + muted rose — the one deliberate exception to "one accent," since distinguishing chart series is a different problem than branding). Added a `--radius: 0.5rem` (8px, down from the shadcn-scaffold default 0.625rem) and a new `--shadow-card`/`--shadow-dropdown`/`--shadow-modal` elevation scale (didn't exist before — the prior app had no named shadow tokens, just ad hoc `shadow-xl`/`shadow-2xl`), with shadow *color* itself swapping between a soft dark tint (light mode) and true black (dark mode) via `--shadow-color`/`--shadow-color-strong`, since a shadow authored only for light mode is invisible on a dark canvas.
  - **Fonts**: swapped the nova preset's `@fontsource-variable/geist` for `@fontsource-variable/archivo` (display/heading), `@fontsource-variable/public-sans` (body/UI), `@fontsource-variable/jetbrains-mono` (tabular data) — verified the actual shipped `font-family` names (`'Archivo Variable'`/`'Public Sans Variable'`/`'JetBrains Mono Variable'`) rather than assuming, wired into `@theme inline` as `--font-heading`/`--font-sans`/`--font-mono`.
  - **Gotcha worth remembering**: an em-dash (—) inside a plain CSS `/* */` comment broke Tailwind v4's Lightning-CSS-based parser with a cryptic "Unterminated string" error pointing at unrelated text. Fixed by rewriting the comment in plain ASCII (hyphens, no smart punctuation) — **avoid em-dashes/smart quotes inside this app's `.css` files going forward**, plain ASCII only in CSS comments.
  - **`src/contexts/ThemeContext.tsx`** (renamed from `.jsx`, fully rewritten — it had zero consumers anywhere in the app, confirmed by grep, so this was a green-field rewrite not a careful migration): three-state preference (`'light' | 'dark' | 'system'`) distinct from the two-state *resolved* theme, persists explicit choices to `localStorage` under `management-theme`, defaults to `prefers-color-scheme` when unset, re-subscribes to OS changes only while `'system'` is active (an explicit choice shouldn't silently start following the OS again), toggles the `.dark` class on `document.documentElement`. Wired `ThemeProvider` into `src/main.jsx` (outermost, before `AuthProvider`/`PermissionProvider`) — it wasn't mounted anywhere before either.
  - **New test suite**: `src/contexts/__tests__/ThemeContext.test.tsx` (6 tests — OS-default resolution both ways, explicit choice overrides + persists, reads a stored preference on mount, switching back to `'system'` re-subscribes to a live OS change, throws outside a provider). This is the "verified light/dark" gate for 1.6 rather than eyeballing — jsdom has no real `matchMedia`, so tests stub `window.matchMedia` directly (not `vi.spyOn`, which fails when the target property doesn't already exist).
  - **`Management/DESIGN.md`** (new): full frontmatter (`colors`/`colors-dark`/`typography`/`rounded`/`spacing`/`components`) + all spec-ordered body sections. Ran `npx @google/design.md lint DESIGN.md` for real (the tool exists on npm, `@google/design.md`) — **0 errors**, 22 warnings, all expected/documented: most tokens are "orphaned" since only `Button` is documented in `components:` so far (resolves naturally as Phase 2 lands more primitives), and `colors-dark` isn't part of the base spec's schema (no native light/dark mechanism exists yet upstream) so it's flagged as an unrecognized-but-harmless extension key — noted in the file's own Known Gaps section rather than silently accepting a mismatch.
  Verified: `npm run typecheck` ✓, `npm run lint` ✓ (147/0, unchanged), `npm test` ✓ (266/266, +6 new), `npm run build` ✓ (confirmed our exact hex values and font names present in compiled CSS output), `npm run dev` boots clean.
  Next: **Phase 2 — Shared Primitive Library.** Start with core `components/ui/` primitives (Card, Dialog, Table, Badge, Input/Select/Textarea, Tabs, Popover — Button already exists from Phase 0.4), then the `StatCard` consolidation, then the `react-hook-form`+zod `Form` wrapper, then `Sidebar.jsx`/`ProtectedRoute.jsx` migration (both under continuous `rbac.spec.js` e2e coverage — run `npm run test:e2e` against those, not just Vitest, before considering that step done).

- **2026-08-22 (same session, continued further)**: Completed 2.1 via `npx shadcn@latest add card dialog table badge input select textarea tabs popover -y` — 9 new files in `src/components/ui/`, zero new dependencies needed (everything Base UI/cva-related was already installed from Phase 0.4). Documented every one of them in `DESIGN.md`'s Components section with what it's for and what still needs to be built on top (the `DataTable` wrapper on top of raw `table.tsx`, the `Form`/`FormField` wrapper on top of raw `input`/`select`/`textarea`) — noting the gap explicitly rather than pretending the raw primitive is the finished story. `badge.tsx` picked up the same expected `react-refresh/only-export-components` warning as `button.tsx` (exporting both the component and its `cva` variants fn) — harmless, matches upstream shadcn convention, already accepted for button.tsx.
  Verified: `npm run typecheck` ✓, `npm run lint` ✓ (149/0 — 2 new expected warnings, both the same accepted badge/button pattern), `npm run build` ✓, `npm test` ✓ (266/266, unchanged - no new tests needed for unmodified-from-registry primitives), `npx @google/design.md lint DESIGN.md` ✓ (still 0 errors).
  Next: 2.2 (`StatCard` primitive - build now even though deleting the two old duplicates waits for Phase 4/5's `analytics`/`user-management` migrations), 2.3 (`DataTable` wrapper on `table.tsx`), 2.4 (`react-hook-form` + zod `Form` wrapper — needs `npm install react-hook-form @hookform/resolvers`), 2.5 (`Sidebar.jsx`/`ProtectedRoute.jsx` migration — the riskiest remaining Phase 2 step since both are under continuous `rbac.spec.js`/`login.spec.js` e2e coverage; that suite needs the full local stack up (`cd Services && npm run dev`) to actually run, which hasn't been done yet this session — start Phase 2.5 by checking whether that stack is already running or needs starting).

- **2026-08-22 (same session, continued further still)**: Completed 2.2-2.4.
  - **`src/components/shared/StatCard.tsx`**: read both old duplicates in full first (`features/analytics/.../StatCard.jsx` and `features/user-management/.../StatsCard.jsx`) to build a genuine superset rather than guessing — `icon, label, value, unit, subtitle, trend, trendDirection, color, loading`. Built on `Card`/`CardContent`. Deliberately kept trend color (`success`/`destructive`) independent of the card's own `color` prop (`primary|success|warning|destructive|muted`, soft `bg-{token}/10` fills) — state and branding are different things, per `DESIGN.md`'s Do's and Don'ts. `value` renders `font-mono tabular-nums`. Covered by 6 tests (`src/components/shared/__tests__/StatCard.test.tsx`).
  - **`src/components/shared/DataTable.tsx`**: typed wrapper over the raw `table.tsx` primitives adding loading/empty/sortable-numeric conventions the primitive doesn't have on its own. Sort state lives with the caller (`onSort(key)` callback), not the table — matches how the app already fetches pre-sorted pages server-side. Covered by 6 tests.
  - **`src/components/ui/form.tsx`**: discovered mid-step that `npx shadcn add form` is a no-op in this shadcn version (silently does nothing, no error) — the actual registry item is called **`field`**, and it ships as bare, form-library-agnostic `Field`/`FieldLabel`/`FieldError`/etc. primitives (no bundled RHF wiring at all, unlike the classic shadcn `form.tsx`). Ran `npx shadcn add field` instead (also pulled in `label.tsx`/`separator.tsx` as its own deps), then hand-built the RHF binding layer: `Form` (renamed `FormProvider`), `FormField` (wraps `Controller`), `FormFieldItem` (the actual per-field wrapper - takes `label`/`error`/`children`, wires `Field`'s `data-invalid` and `FieldError`'s `errors` prop from RHF's `fieldState.error`). Installed `react-hook-form` + `@hookform/resolvers` (confirmed its `zod` peer range `^3.25.0 || ^4.0.0` covers the app's already-installed zod v4). **Verified with a real end-to-end test**, not just typechecked: a genuine `useForm` + `zodResolver` + submit cycle asserting actual validation message text and that `onSubmit` only fires on valid data (5 tests, `src/components/ui/__tests__/form.test.tsx`).
  - Documented all three in `DESIGN.md`'s Components section.
  Verified: `npm run typecheck` ✓, `npm run lint` ✓ (149/0, unchanged from 2.1), `npm run build` ✓, `npm test` ✓ (283/283, +17 from this step), `npx @google/design.md lint DESIGN.md` ✓ (still 0 errors).
  Next: **2.5 — `Sidebar.jsx`/`ProtectedRoute.jsx` migration.** This is the one Phase 2 step under direct, continuous Playwright coverage (`e2e/auth/rbac.spec.js`, `e2e/auth/login.spec.js`) — before starting, check whether the local microservices stack (`cd Services && npm run dev`, gateway on :3000) is already running; `npm run test:e2e` needs it live and must not point at production. This is genuinely the highest-risk single step in the whole plan so far — the first one that can break real navigation/access-control if done carelessly.
  **Session paused here at the user's explicit choice** (checked all relevant ports — nothing running; asked whether to start the stack myself, have the user start it, skip live e2e for now, or pause — user chose to pause). Phases 0-2.4 are fully committed and green (9 commits on `feat/management-ui-design-system-rewrite`, based off `microservices`). **Nothing is broken or half-finished** - this is a clean stopping point, not an interruption mid-task. To resume: start the local stack (`cd Services && npm run dev`), then begin 2.5 as planned above.

- **2026-08-22 (new session)**: Resumed. Started the local stack (`cd Services && npm run dev`, all 11 services + gateway healthy) and Management's dev server (`:5174`). **Completed 2.5 — `Sidebar.jsx`→`.tsx` and `ProtectedRoute.jsx`→`.tsx`.**
  - Confirmed scope first: `DESIGN.md`'s own Known Gaps section said explicitly "the actual `Sidebar.jsx` component doesn't migrate until Phase 2, and its concrete layout/interaction spec belongs in this file once that happens" — so 2.5 is a real visual migration to Signal Console tokens, not just a file-extension rename. Proceeded on that basis.
  - **`Sidebar.tsx`**: converted to TS (typed `NavItem[]`, `OrgBranding`, local state). Replaced every hardcoded sky/blue gradient and decorative blur element with the dedicated `sidebar`/`sidebar-foreground`/`sidebar-primary`/`sidebar-accent`/`sidebar-border` token set already defined in `src/index.css` (was defined in Phase 1 but unused until now). Active nav item: solid `bg-sidebar-primary` fill (was a blue gradient + `shadow-lg`). Brand tile: flat `bg-sidebar-primary` square, `rounded-lg` (was a gradient `rounded-2xl` tile with `hover:scale-105`/`shadow-xl`). Removed the two decorative blurred-circle background divs entirely (retired per `DESIGN.md`'s "Sharp, not bubbly" / no-gradient rules). Sign Out is now the shared `Button` (`variant="destructive"`, soft-fill) instead of a hand-rolled button with inline `rgba()` border. Role badge switched from an inline `⭐ Super Admin` emoji-in-span to the shared `Badge` (`variant="secondary"`) plus a `Sparkles` icon for `superAdmin` only — visual-only change, no test asserts on it. Collapse toggle and mobile hamburger kept as plain icon buttons (not the `Button` primitive — its fixed action-button sizing didn't fit either), restyled onto `card`/`border`/`muted-foreground` tokens, `hover:scale-110` dropped.
  - **Functional behavior preserved exactly** (not in scope to change): permission/role filtering logic, mobile-breakpoint detection, auto-collapse-after-nav-click on desktop, org-branding fetch/fallback, logout flow, greeting-by-time-of-day. All nav item accessible names (`getByRole('button', { name: ... })` in `rbac.spec.js`/`login.spec.js`) and the `Access Denied` heading text/role are unchanged — no spec updates were needed.
  - **`ProtectedRoute.tsx`**: converted to TS (typed props), restyled the loading spinner (`text-primary`, was `text-blue-600`) and the Access Denied screen (`font-heading` display-scale heading, `muted-foreground` body text) onto tokens. Same behavior, same `Access Denied` text/heading role. Updated its test's import path (`../ProtectedRoute.jsx` → `../ProtectedRoute.tsx`) — no assertions changed.
  - **Found and fixed a real, pre-existing bug while here**: `src/index.css` had a stray unlayered `body { font-family: -apple-system, ... }` rule (left over from before Phase 1) that — because Tailwind's `@layer base` styles always lose to unlayered ones regardless of source order — was silently overriding the entire `font-sans`/`font-heading` system app-wide ever since Phase 0.4. This was flagged as a known issue in that session's own notes ("nothing renders it yet ... inert until Phase 1's real typography decision") but never actually fixed in Phase 1. Fixed by deleting the stray rule and folding `antialiased`/`margin:0` into the proper `@layer base` block. **Verified in the actual build output**: `dist/assets/index-*.css` now shows `html{font-family:Public Sans Variable,sans-serif}` where before the stray rule would have won. This means Public Sans/Archivo are only now rendering for real, app-wide — not scoped to Sidebar, but caught and fixed as part of this step since Sidebar is the first component that would have visually surfaced it.
  - Deleted both old `.jsx` files (extensionless imports in `App.jsx` needed no changes).
  - Updated `DESIGN.md`: new "Sidebar / App Shell" entry under Components documenting the token choices and why (e.g. dedicated `sidebar-*` tokens instead of `card`, solid vs. soft-tint active state), removed the now-resolved Known Gaps line about Sidebar. `npx @google/design.md lint DESIGN.md` — 0 errors (same warning count/shape as before, all pre-existing/expected).
  - **Verified**: `npm run typecheck` ✓, `npm run lint` ✓ (149/0, unchanged), `npm run build` ✓ (confirmed Public Sans in compiled CSS as above), `npm test` ✓ (283/283, unchanged — the `useTheme must be used within a ThemeProvider` console errors during the run are expected noise from `ThemeContext.test.tsx`'s own negative test, not a new failure). `npm run test:e2e -- e2e/auth` (the actual 2.5 gate) — **10/10 passing** (`login.spec.js` + `rbac.spec.js`), confirming role-gated nav visibility and the Access Denied screen both survived the migration unchanged. A follow-up full-suite `test:e2e` run (including `lead-lifecycle.spec.js`) hit the Gateway's 10-req/15min auth rate limit from running the auth suite twice back-to-back in the same session — expected per the fixture's own documented limit, not a regression; not re-run this session (`lead-lifecycle.spec.js` isn't gated on this step anyway — that's a Phase 4 hard gate).
  Next: **Phase 3 — Feature Migration, Wave 1**: `features/dashboard` (3 files), then `features/career` (3 files).

- **2026-08-22 (new session, continued)**: **Completed 3.1 — `features/dashboard`.** `DashboardContainer.jsx` → `.tsx`, full restyle:
  - **Replaced the hand-rolled `MetricTile` with the shared `StatCard`** (Phase 2.2) instead of retoking it in place — this is exactly the kind of duplication `StatCard` was built to absorb. Mapped each metric's old ad hoc `colorScheme` (blue/emerald/violet/amber/indigo/cyan Tailwind stock colors) to `StatCard`'s 5 real tokens: Total Leads→`primary`, Active/Total-count tiles→`muted`, Monthly Revenue/Active Users→`success`, Outstanding→`warning` (matches DESIGN.md's own "warning: pending payment" guidance exactly). Same treatment for the sales-rep performance mini-cards and the two `RevenueSummaryCards`.
  - **`ChartPanel` rebuilt on `Card`/`CardHeader`/`CardTitle`/`CardDescription`/`CardContent`** instead of a hand-rolled `bg-white rounded-2xl` div.
  - **`TimeSelector` replaced with the shared `Tabs`/`TabsList`/`TabsTrigger`** primitive (Phase 2.1) used as a controlled segmented control (no `TabsContent` panels — the selected value drives data fetching elsewhere, not tab-scoped content, which is a legitimate way to use Base UI's Tabs).
  - **Resolved the "Chart component styling isn't specified yet" Known Gap** (recharts axis/gridline/tooltip treatment) that DESIGN.md flagged as due at this exact step. recharts takes literal CSS color strings, not Tailwind classes, so grid/axis/tooltip and the 4-series lead-funnel chart, the 2-series revenue bars, and the 5-segment destination radial chart all reference `var(--color-chart-1..5)`/`var(--color-border)`/`var(--color-muted-foreground)`/`var(--color-popover)`/`var(--shadow-dropdown)` directly as inline color strings — confirmed this correctly re-resolves live on theme toggle (screenshotted both themes, no stale colors). The old two-hue decorative bar gradient (indigo→violet on a single Revenue series) is gone, replaced by one solid `chart-2` fill — multi-hue gradients on a single series were exactly the kind of decoration DESIGN.md retires; a distinct second hue for the separate Target series is the correct multi-series use.
  - Documented this pattern (CSS-var color props for recharts, not Tailwind classes) in `DESIGN.md`'s Known Gaps as the now-established approach, so `features/analytics` (Phase 3.2... corrected below, Phase 4.1) can reuse it directly instead of re-deriving.
  - Dropped two recharts `RadialBar` props (`minAngle`, `clockWise`) that TypeScript's real type-checking caught as not existing anywhere in this project's installed recharts 2.15.3 types — dead props that silently no-op'd at runtime under the old untyped `.jsx` file. Real bug-catching value from the TS migration itself, not a behavior change (confirmed visually unchanged).
  - **New warnings, accepted deliberately**: 18 net new `@typescript-eslint/no-explicit-any` warnings (149→167 total, still 0 errors) from the dashboard's several genuinely-untyped nested API response shapes (`leadData?.trend`, `destinationPerformance`, etc. — the services layer itself is still untyped `.js`, so there's no real interface to type against yet). Per the plan's own strictness ramp ("non-strict now, tighten after Phase 2, full `strict: true` after Phase 5"), building full response-shape interfaces now is out of scope for what's fundamentally a visual/token migration step — flagging rather than either suppressing or over-building.
  - Test file import path updated (`DashboardContainer.jsx` → `.tsx`); no assertion changes needed — `StatCard`'s rendered value/trend text matches the original plain-text output exactly, including the `getAllByText('12')` duplicate-value case (top metric card + `UserAnalyticsPanel`'s own mini box).
  - **Caught and fixed a Vite dev-server footgun while testing visually**: swapping a file from `.jsx`→`.tsx` via separate delete+create (rather than a plain rename) left Vite's running dev server with a stale module-graph entry pointing at the deleted `.jsx` path, producing a blank white page even though build/typecheck/tests were all green. Fix is a plain dev-server restart (not a code bug) — noting it here since Phase 3.2+ will hit the same pattern on every remaining `.jsx→.tsx` feature file and should expect to restart `npm run dev` after each one before visually spot-checking.
  - Verified: `npm run typecheck` ✓, `npm run lint` ✓ (167/0 — 18 new accepted `any` warnings, explained above), `npm run build` ✓, `npm test` ✓ (283/283, unchanged), visually confirmed in both light and dark via a real logged-in screenshot (StatCard tokens, chart colors, and Tabs selector all correct in both themes).
  Next: **3.2 — `features/career`** (3 files: `CareerContainer.jsx`, `VacanciesContainer.jsx`, `index.js`).

- **2026-08-22 (new session, continued)**: **Completed 3.2 — `features/career`.** `CareerContainer.jsx`/`VacanciesContainer.jsx` → `.tsx`, full restyle. This was the first migration to reach for `Dialog`, `Select`, and `DataTable` for real (Phase 2's Card/Table/Badge/Form had only been unit-tested, never exercised live in a browser) - every hand-rolled `fixed inset-0 bg-black/50` modal (View Details, Edit Status, Vacancy Form, Delete Confirm) is now `Dialog`/`DialogContent`/`DialogHeader`/`DialogFooter`; the 5 hand-rolled stat tiles → shared `StatCard` (Total→muted, Pending→warning, Shortlisted→success, Rejected→destructive, Hired→primary); the applications table → shared `DataTable`; status/position pills → `Badge` composed with semantic tokens (position tags rotate `chart-1..5` - documented as a new reusable pattern in `DESIGN.md`'s Badge section, since it's a legitimate categorical, non-state use).
  - **Found and fixed three real, pre-existing bugs, verified live against the running stack (not guessed):**
    1. **`fetchStats()` parsed a MongoDB-era response shape that no longer matches.** The live `/careers/stats` endpoint (Prisma/Postgres) returns `{ total, byStatus: [{status, _count}] }`; the code (unchanged since before this migration) was reading `statsData.totalApplications?.[0]?.count` and `byStatus.find(s => s._id === ...)`. Every stat silently rendered `0` - confirmed via `curl` against the live endpoint (`{"total":59,"byStatus":[{"_count":25,"status":"pending"}, ...]}`) before and after the fix.
    2. **Every application/vacancy record reference used `_id`, but the real API returns `id`** (confirmed via `curl` - `'_id' in apps[0]` is `false`, `'id' in apps[0]` is `true`). This meant `getRowKey`, View/Edit/Delete handlers, and the position-filter dropdown were all keying/acting on `undefined` - View/Edit/Delete on career applications and vacancies were non-functional against the live backend before this fix, and it's also what caused a very real "Each child in a list should have a unique key prop" React warning (confirmed reproducible with a live, rate-limit-cleared fetch, not just a screenshot artifact) that went away once `getRowKey` returned real per-row `id`s.
    3. **Vacancy `experience.min` doesn't exist on the wire.** The Prisma schema flattens it to `experienceMin` (`schema.prisma`'s own comment says so: "flattened from experience.min"), and `vacancy.controller.js` reads `req.body.experienceMin` directly - the nested `{ experience: { min } }` shape the form built and sent was silently dropped on create/update, and display read `undefined+ years`. Fixed the `VacancyFormData`/`Vacancy` TS shapes and every read/write site to use flat `experienceMin`, matching the real contract on both ends (confirmed via the Prisma schema and controller source, not guessed).
  - **Found and fixed one real bug in a Phase 2.1 primitive, not career code**: `src/components/ui/button.tsx`'s `Button` was a plain function component, not wrapped in `React.forwardRef`. This is invisible until something tries to attach a ref to it - which `DialogClose`'s default close-button (`render={<Button variant="ghost" .../>}`) does, via Base UI's `render`-prop merging, for its internal focus-management. Every dialog in the app (Phase 2.1 onward) had a broken default close "X" button; nothing caught this earlier because Phase 2.1-2.4 verification was typecheck/lint/build/unit-test only, and this is the *first* migration to actually click a live `Dialog` in a browser. Fixed by wrapping `Button` in `React.forwardRef` (Base UI's own `ButtonPrimitive` already forwards refs correctly - confirmed via its `.d.ts`). This fix benefits every future `Dialog` usage app-wide, not just career.
  - All three career-data bugs were confirmed via direct `curl` against the live local stack (gateway rate-limit reset by touching `Services/gateway/src/index.js` to force a nodemon restart, which clears its in-memory `express-rate-limit` store - documented here since it came up twice this session) - not guessed from reading code alone.
  - **Not fixed, flagged for a future dedicated pass**: nothing else found beyond the three above; the rest of `careerService`'s methods (`updateApplicationStatus`, `deleteApplication`, `createVacancy`, `updateVacancy`, `deleteVacancy`) were exercised structurally (types, live GET calls) but not full round-tripped (no vacancies were seeded to test create/edit/delete against; application mutations weren't performed to avoid mutating shared seed data outside of the dedicated `Services/e2e-tests` suite's tagging/cleanup convention). If a future session touches this feature again, verify those write paths live before assuming they're correct now.
  - Verified: `npm run typecheck` ✓, `npm run lint` ✓ (166/0 - net *fewer* warnings than before this step, despite two new `.tsx` files, since more `any`/unused-var warnings were removed from the old files than were added), `npm run build` ✓, `npm test` ✓ (283/283, no existing career tests to break), and real logged-in-as-superAdmin screenshots: applications table with live corrected stats/data, the View Details dialog, the Vacancies tab (empty state), and the Create Vacancy dialog - all in light mode, all clean, zero console errors on the final pass (the key-prop warning and the ref warning were both still reproducing on the *first* screenshot pass and gone by the final one, confirming the fixes rather than assuming them).
  Next: **Phase 4 — Feature Migration, Wave 2**: `features/analytics` (31 files, delete the duplicate `StatCard.jsx`), `features/shared` (8 files), `features/itinerary` (45 files), `features/lead-management` (42 files, hard `test:e2e` merge gate). Given `features/analytics` reuses the same recharts theming pattern this session established for `features/dashboard`, and is next in the plan's own ordering, that's the natural starting point.

- **2026-08-22 (new session, continued)**: **Completed 4.1 — `features/analytics`.** All 6 page components (`MyPerformanceAnalytics`, `UserAnalytics`, `LeadAnalytics`, `BillingAnalytics`, `ItineraryAnalytics`/`PackageAnalytics`, `WebsiteAnalytics`) plus the shared `Common/` building blocks migrated to `.tsx`/`.ts`.
  - **`Common/` rebuilt first, once, for all 6 pages to reuse**: `ChartContainer` → `Card`/`CardHeader`/`CardTitle`/`CardDescription`/`CardContent` (same shape as `features/dashboard`'s local `ChartPanel`, kept feature-local rather than promoted to `components/shared/` — it's analytics' own internal building block, not reused outside this feature, so promoting it would be an unrequested abstraction). `TimeRangeFilter` → shared `Tabs` used as a controlled segmented control (Daily/Weekly/Monthly/Annual). All 4 `Charts/*` wrappers (`Area`/`Bar`/`Line`/`Pie`ChartComponent) retheme onto a new `Common/chartTheme.ts` (grid/axis/tooltip/legend CSS-var constants + the `chart-1..5` `CHART_PALETTE` array) - the single source every chart in this feature (and `features/dashboard` already, informally) should reference, rather than each `.tsx` re-deriving the same `var(--color-chart-1)` strings. `Common/StatCard.jsx` **deleted** per plan; all 6 pages repointed to the shared `components/shared/StatCard`, mapping each page's old ad hoc 8-color palette (`blue`/`green`/`purple`/`orange`/`red`/`pink`/`cyan`/`indigo`) onto the shared component's 5 real tokens case-by-case per metric's actual meaning (e.g. Billing's "Outstanding" → `warning`, never a fixed 1:1 name rename).
  - **Found and fixed a real, live-verified bug this step would otherwise have silently introduced**: `features/analytics/utils/exportAnalytics.js`'s PDF-export chart-capture (`captureAllCharts`) queried `document.querySelectorAll('.bg-white.rounded-2xl.border')` - a literal copy of `ChartContainer`'s *old* Tailwind classes. Migrating `ChartContainer` onto `Card` (which renders different classes entirely) would have made this selector match zero elements, silently breaking every analytics page's "Export PDF" button (0 charts captured, PDF still "succeeds" but contains none). Fixed by switching to `[data-slot="card"]` - the semantic marker `Card` itself sets (`src/components/ui/card.tsx`), immune to future className churn. Same issue existed for the per-chart *title* lookup (`h2, h3, h4, [class*="title"]` - `CardTitle` renders a `<div data-slot="card-title">`, not a heading, so titles would have silently fallen back to generic "Chart 1"/"Chart 2" labels); fixed the same way, keeping the old selectors as a fallback for any not-yet-migrated page. This file already had a regression test guarding exactly this class of bug from a *prior* incident (`captureAllCharts selector` › "not the old rounded-lg selector") - updated that test's DOM fixture to construct the real new shape (`data-slot="card"` root, `data-slot="card-title"` div instead of `<h2>`) rather than declare victory against a fixture that would no longer represent production markup. Converted the file to `.ts` in the same pass (dropped `extractSVGFromChart`/`captureCharts`, confirmed genuinely dead code via a repo-wide grep, not just unused in this file).
  - **`features/analytics/index.js` migrated to `.tsx` too, then discovered to be dead code** - grepped for consumers and found none; the actual page mounted at `/analytics` (via `App.jsx`) is `src/pages/Analytics.jsx`, a *different*, much larger component with its own hand-rolled responsive (mobile-tabs / desktop-sidebar) navigation shell wrapping the same 6 analytics components. Left the now-modernized-but-still-unreferenced `features/analytics/index.tsx` in place (not newly-introduced dead code, just improved existing dead code; deleting a pre-existing file outside what was asked felt like the wrong call to make unilaterally) - flagging it here as a real candidate for Phase 6's cleanup sweep.
  - **Migrated the *actual* consumer, `src/pages/Analytics.jsx` → `.tsx`, even though `src/pages/*` is nominally Phase 6.3 scope** - deliberate, scoped exception: leaving the real integration shell on old gradient/slate styling would have wrapped this step's freshly-restyled 6 analytics pages in a visibly inconsistent frame, undermining the whole point of the phase. Replaced each tab's distinct hardcoded accent hex (`#3b82f6`, `#10b981`, `#8b5cf6`, ...) with the single `primary` token for the active-nav-item state (matching `Sidebar.tsx`'s own convention) - six different "brand" colors per tab was exactly the kind of thing Signal Console's "one accent" rule retires; DESIGN.md never sanctioned per-tab branding, so this is a deliberate simplification, not an oversight.
  - **`features/analytics/index.tsx`'s own tab bar**: discovered its `Tabs`/`TabsList`/`TabsTrigger`/`TabsContent` were already imported (leftover Phase 0.4 scaffolding, via the `@/` alias) but never actually used - the real tab UI was hand-rolled buttons + `style={{ display: ... }}`. Wired them up for real (`variant="line"`) instead of leaving another unused import behind. Preserved the original's "every tab stays mounted, only one visible" behavior with `keepMounted` on each `TabsContent` - Base UI's default is to unmount inactive panels, which would have silently changed behavior to "refetch on every tab switch" instead of "fetch once, keep in memory." A behavior change like that is a product decision, not a side effect of a styling migration.
  - Utils: `userAnalyticsData.js` → `.ts` (light typing over the existing mock-data/aggregation logic, no behavior change). Noted but did not remove: `getAggregatedUserStats` turned out to be genuinely dead code (confirmed via grep) even in the *original* `UserAnalytics.jsx` - its result was computed into a `userStats` variable that was never actually read anywhere in that component's render. Left the now-truly-unused export in place rather than expanding this step's diff with an unrelated dead-code removal; flagging for whoever next touches this file.
  - Verified: `npm run typecheck` ✓, `npm run lint` ✓ (191/0 - all net-new warnings are the same accepted `@typescript-eslint/no-explicit-any` category as every prior `.tsx` step), `npm run build` ✓, `npm test` ✓ (283/283, all 6 pre-existing analytics test suites plus `exportAnalytics.test.js` still pass against the migrated components/fixtures with zero assertion changes needed beyond import-path/DOM-fixture updates). Live-verified in the browser as superAdmin: both the Lead Analytics and Billing Analytics tabs in light and dark, confirmed via `page.evaluate` that `[data-slot="card"]` now correctly finds exactly the chart-containing cards (8 total cards, 4 chart cards on the Billing tab - matching its 4 real charts), and confirmed zero console errors on the final pass.
  Next: **4.2 — `features/shared`** (8 files: `FlightSelectionModal`, `HotelSelectionModal`, `FlightPreferenceCard`).

  **Session paused here at the user's request**, moving to a fresh session for the rest of Phase 4. Phases 0-4.1 are fully committed and green (13 commits on `feat/management-ui-design-system-rewrite`, based off `microservices`; latest is `ab0bc07`). **Nothing is broken or half-finished** — working tree is clean, `typecheck`/`lint`/`test`/`build` all pass. Two things worth knowing before resuming:
  - The local microservices stack + Management dev server were running throughout this session (started via `cd Services && npm run dev` and `cd Management && npm run dev`) for live verification — check `lsof -i :3000` / `:5174` before starting a fresh copy, and if the gateway's rate limiter (300 req/15min global, 10/15min auth) is exhausted from a prior session's testing, `touch Services/gateway/src/index.js` force-restarts it under nodemon and clears the in-memory limiter (came up twice this session).
  - Whenever a file is swapped `.jsx`→`.tsx` via delete+create while the Vite dev server is already running, its module graph goes stale and serves a blank page even though `typecheck`/`build` are green — restart `npm run dev` after each such swap before screenshotting, not just after every phase.
  Resume by reading this file top to bottom (per this file's own header instruction), then starting 4.2.

- **2026-08-23 (new session)**: **Completed 4.2 — `features/shared`.** `flightLegDefaults.js`→`.ts` (added `FlightLegPrefs` interface, otherwise untouched — pure logic, already had full test coverage), `FlightPreferenceCard.jsx`→`.tsx`, `FlightSelectionModal.jsx`→`.tsx`, `HotelSelectionModal.jsx`→`.tsx`, `index.js`→`.ts` (barrel, no changes needed - imports are already extensionless).
  - **Both modals rebuilt on `Dialog`/`DialogContent`/`DialogHeader`/`DialogTitle`/`DialogDescription`** (Phase 2.1's primitive, first exercised for real by `features/career` in Phase 3.2) - replacing every hand-rolled `fixed inset-0 bg-black/40 backdrop-blur-sm` + `bg-white rounded-2xl shadow-xl` wrapper. `DialogContent`'s default built-in close button (`showCloseButton`) replaces both modals' manual `<button onClick={onClose}><X/></button>` - one fewer hand-maintained affordance per modal. Removed the `if (!isOpen) return null` early return in `FlightSelectionModal` (previously needed since the component built its own overlay); `Dialog`'s `open`/`onOpenChange` now owns visibility, matching Career's established `open={showX} onOpenChange={setShowX}` pattern - here it's `open={isOpen} onOpenChange={(open) => { if (!open) onClose(); }}` since `isOpen`/`onClose` are parent-controlled rather than local state.
  - **`HotelSelectionModal` gave up its own accent color entirely.** The original used a dedicated amber/orange gradient header and amber buttons/borders throughout (`from-amber-600 to-orange-600`, `text-amber-600`, `hover:border-amber-300`, etc.) - a second brand hue alongside Flight's blue, which is exactly what DESIGN.md's "don't introduce a second accent hue" rule retires. Every one of those is now `primary` (teal), matching `FlightSelectionModal` and the rest of the app - hotel and flight booking flows now look like the same product instead of two different sub-apps bolted together.
  - **New pattern, documented in `DESIGN.md`'s Components section**: `HotelCard`'s `StarRating` (Phase 4.2's first star-rating UI anywhere in the app) fills stars with `text-warning`/`fill-warning` rather than inventing a dedicated gold/amber token - `warning`'s hue already reads as amber and matches the universal rating convention, and DESIGN.md's own "don't invent a new gray" principle extends naturally to "don't invent a new hue for one decorative use." Empty stars use `text-muted`. Scoped narrowly to rating displays in the new DESIGN.md entry, not a general license to reach for `warning` outside state/rating.
  - **Badges**: `Refundable` → `bg-success/10 text-success` (state - a real fare attribute). `Direct` (flight) → `bg-primary/10 text-primary` (informational highlight, not state - matches how `Best Match` on hotels uses a solid `bg-success text-success-foreground`, both are "this one stands out" callouts rather than a lifecycle status).
  - **Trip-type toggle → `Tabs`/`TabsList`/`TabsTrigger`** (Phase 2.1 primitive, controlled, no `TabsContent` panels needed - same "segmented control, not real tabs" usage as `features/dashboard`'s `TimeSelector` and `features/analytics`'s `TimeRangeFilter`). Cabin class / departure time / traveler title / gender / guest title / adults-children counts all became `Select`/`SelectTrigger`/`SelectContent`/`SelectItem`, matching `features/career`'s established `<label className="...uppercase tracking-wide...">` + bare `Select`/`Input` convention (no `react-hook-form`/`FormField` wrapper - these are large, multi-step, imperative wizards driven by plain `useState`, not a single validated form, so Career's simpler convention is the right fit, not Phase 2.4's RHF binding).
  - **Base UI `Select` gotcha, worth remembering for Phase 4.3+**: empty string is not a valid controlled `value` for "nothing selected" - `Select.Root.value` wants `null`/`undefined` for that, which then triggers `SelectValue`'s dedicated `placeholder` prop (confirmed via `@base-ui/react`'s own `.d.ts`, not guessed). Used `value={t.gender || null}` + `<SelectValue placeholder="Gender *" />` for the genuinely-unset traveler-gender field. Where the original had `""` as an actual **selectable, always-visible** option (`<option value="">Any time</option>` for departure-time preference - a real choice the user can pick and see reflected, not just an empty/placeholder state), an empty string still doesn't work as an `Item` value, so that case uses a sentinel (`'__any__'`) mapped to `''` at the read/write boundary instead - the two cases look similar but needed different fixes because they mean different things ("nothing chosen yet" vs. "user chose the deliberately-blank option").
  - **Two real, pre-existing bugs found and fixed in shared primitives while migrating** (not new code from this step, but blocking issues this migration surfaced by being the first `.tsx` consumer):
    1. **`AirportAutocomplete.jsx`**: a malformed JSDoc `@param {"popular"|"all"} listMode` comment was making TypeScript infer the *entire* props type as the literal union `"popular" | "all"` for any `.tsx` caller (not an object type at all) - every prop passed from a typed consumer failed to typecheck. Rewrote the comment as plain prose (dropping the `@param` tag) so TS falls back to its normal per-parameter inference.
    2. **Same file**: `label`, `id`, `excludeCode`, `prioritizeCountry` were destructured without a default value, which TypeScript's JS-inference treats as *required* props (even though the runtime is perfectly happy with them being `undefined`) - added explicit `= undefined` defaults so future `.tsx` callers (this step and, eventually, Phase 4.3/4.4's itinerary/lead-management, which also use this component) don't have to pass them just to satisfy the type checker. Zero behavior change - `undefined` was already the runtime value when omitted.
  - **Not migrated, deliberately out of scope**: `AirportAutocomplete.jsx`, `PassengerSelector.jsx`, `CountrySelect.jsx`, `flight.service.js`, `hotel.service.js` all remain untyped `.jsx`/`.js` - they're consumed by, not owned by, `features/shared`, and none of the plan's phase boundaries assign them here. `FlightOffer`/`Hotel`/booking-response shapes in both modals are typed loosely (`any` on catch-block errors and raw API response data) for the same reason `features/dashboard`/`features/analytics` accepted `any` at their untyped-backend boundaries - 10 net new `@typescript-eslint/no-explicit-any` warnings (191→197, still 0 errors), all at genuine untyped-service boundaries, not sloppiness.
  - Verified: `npm run typecheck` ✓, `npm run lint` ✓ (197/0 - the 10 new `any` warnings explained above, everything else pre-existing/unrelated), `npm run build` ✓, `npm test` ✓ (283/283, unchanged - no new tests needed since no new behavior, only styling/primitive-swap; both pre-existing test files for this feature still pass against the migrated components with only import-path extension updates), `npx @google/design.md lint DESIGN.md` ✓ (still 0 errors, 22 warnings, same shape as before plus the new Rating Display entry).
  - **Live-verified via a throwaway Playwright script** (not the Chrome extension - that bridge never connected this session; see below), driving the real local stack as superadmin, screenshotting both `emulateMedia`-forced themes at every step. This caught a real bug the static checks couldn't:
    - **`SelectValue` silently rendered raw sentinel/enum values instead of labels** wherever a `Select`'s value and its display label differ - "Preferred Time" showed the literal string `__any__` instead of "Any time"; the same defect was latent (unexercised by the automated test suite, which doesn't open these dropdowns) in traveler Gender (`M`/`F` vs "Male"/"Female") and the hotel Guests adults/children counts (`"2"` vs "2 adults"). Root cause: Base UI's `SelectValue` shows the raw `value` unless given a `children` render-function (`{(value) => ...}`) to map value → label - it does **not** auto-resolve a matching `SelectItem`'s children the way a native `<option>` would. Fixed all four sites with `<SelectValue>{(value) => ...}</SelectValue>`; the two same-value/label selects (Cabin Class, traveler/guest Title) were never affected since raw value and label happened to be identical there. **This is a pattern to check on every future `Select` usage where value ≠ label** (Phase 4.3/4.4 will have more of these) - it typechecks, lints, and unit-tests clean, so only opening the dropdown in a browser reveals it.
    - Confirmed via pixel sampling that the "primary" token resolves to the exact documented hex (`#0f7f8c` light / `#2dd4d9` dark) inside both modals, both themes - not eyeballed.
    - Confirmed live: Dialog's built-in close button, the Tabs trip-type toggle, real hotel search returning 13 live results through `HotelCard` (rank badges, per-night price in mono, "Select"/"View details" in the one accent color, empty-rating state rendering as dashes rather than the star fill), and the real `FlightPreferenceCard` (not the visually-similar-but-unrelated inline flight-summary markup that still lives directly in the not-yet-migrated `ItineraryEditor.jsx`) rendering correctly end-to-end - saved a real inbound preference via `NewLeadDialog`'s Transfer Flights section and watched it render as "SIN → DXB Economy" with the pencil/Remove actions.
    - **One real limitation found and deliberately left unfixed, documented here rather than patched**: `FlightPreferenceCard`, when embedded inside `NewLeadDialog` (not migrated until Phase 4.4), is genuinely hard to read in dark mode - its `text-foreground` correctly resolves to the dark-mode light color (confirmed via pixel sampling: the surrounding app shell, e.g. the sidebar, *does* go properly dark), but `NewLeadDialog` itself is old hardcoded-white markup with no `.dark` awareness, so the card ends up with dark-mode light text on a background that never left light mode - a legibility failure that's an artifact of nesting a finished component inside an unfinished one, not a defect in `FlightPreferenceCard` itself. The old (pre-migration) card used hardcoded `text-gray-900`, which incidentally "worked" in this exact scenario only because it never changed with the theme at all. No fix applied here - the real fix is migrating `NewLeadDialog`/`LeadFlightBookingsSection` in Phase 4.4, at which point this resolves on its own. **Flagging for whoever does 4.4: sanity-check `FlightPreferenceCard` in dark mode once its host dialog is migrated**, since this session couldn't verify the "both sides migrated" end state.
    - Also hit, and worth remembering for future sessions: the gateway's global rate limit (300 req/15min) is easy to exhaust with an iterating screenshot script that reloads/refetches on every run (separate from the already-documented 10/15min auth-only limit) - same `touch Services/gateway/src/index.js` fix applies. Saving Playwright's `storageState` (cookies) to a scratch file after the first login and reusing it across script re-runs avoids re-hitting the *auth* limiter specifically, but doesn't help with the general per-request one.
  Next: **4.3 — `features/itinerary`** (45 files, the largest single step in the plan so far). Given `ItineraryEditor.jsx` is a live consumer of `FlightSelectionModal`/`HotelSelectionModal` in template mode, re-check that flow visually once more as part of 4.3 rather than assuming this session's isolated verification fully covers it in context. Also check every new `Select` usage in this phase for the value-vs-label `SelectValue` gotcha above before considering a Select "done."

- **2026-08-23 (same session, continued)**: **New design rule added before proceeding, triggered by the user's own review of the Phase 4.2 screenshot artifact.** The user caught two real problems the artifact didn't surface on its own: controls sharing a row weren't rendered at the same height (the trip-type `Tabs` next to the compact cabin-class `Select` in `FlightSelectionModal`), and a control's size wasn't consistent with its same-role counterpart elsewhere in the app (the flight modal's trip-type toggle vs. Lead Management's status-filter pills), plus some text rendered too small to comfortably read. A follow-up codebase audit confirmed the pattern precisely: `FlightSelectionModal.tsx:514-528` mixes a fixed-`h-8` `Tabs` with a `Select size="sm"`/`h-7` in one `flex` row; `FlightSelectionModal.tsx:530-590` and `HotelSelectionModal.tsx:398-448` seat legacy `AirportAutocomplete`/`PassengerSelector`/`CountrySelect` (all ≈42-44px, custom `py-2.5` padding, no fixed height) directly beside the new `h-8` `Input`/`Select`/`Button` primitives in the same row; `Sidebar.tsx:248` and `DashboardContainer.tsx:250,254,258` already use `text-[11px]`/`text-[10px]` for real label/caption text, under the type scale's own 12px floor; and `LeadFilters.jsx:54-77`'s not-yet-migrated status pills render ≈36px against the migrated `Tabs`'s fixed 32px — the same *kind* of control, different heights, because nothing ties them to one shared size.
  - Added a new `## Control Sizing & Consistency` section to `DESIGN.md` (between Layout and Elevation & Depth): a named height scale (`{control.default}` 32px/`h-8`, `{control.compact}` 28px/`h-7`, `{control.tight}` 24px/`h-6`) tied to what the primitives already implement, a hard rule that every control sharing one row/toolbar must render at the same tier, a rule that same-role controls (any segmented toggle/filter) must converge on the same size across every screen as each migrates (not just within one screen) - grounded in Nielsen's "consistency and standards" heuristic - and a hard 12px floor on real text, banning arbitrary `text-[Npx]` escapes below `text-xs`. Also added one `Do` and one `Don't` bullet pointing at the new section, and a `Known Gaps` entry naming Phases 0-4.2 as predating the rule and not retroactively fixed. `npx @google/design.md lint DESIGN.md` - still 0 errors, 22 warnings (unchanged shape).
  - **Explicit decision, at the user's direction: phases already completed (0 through 4.2) are left exactly as they are** - their control-sizing debt is not patched piecemeal now. It's tracked as a single new backlog item, **Phase 6.6 - Control-height & sizing-consistency retrofit**, seeded with the concrete file:line instances above so nothing gets lost, and deliberately scheduled *after* Phases 4.3/4.4/5 land (those get built correctly under the new rule from the start; 6.6 only mops up debt that predates the rule). Phase 4.2's own checklist line now points at 6.6 rather than silently looking "done" with no caveat.
  - Two commits from here: the Phase 4.2 code itself (already fully verified before this detour started), then this `DESIGN.md`/progress-file documentation as a separate `docs` commit - so the rule-writing work doesn't hold up code that was already done and tested.
  Next: **4.3 — `features/itinerary`**, same scope as noted above, now explicitly bound by the new Control Sizing & Consistency rule from the start - before considering any row of controls "done," list every control in it, its actual rendered height/size prop, and confirm they match (the same check the audit above did by hand); and check any `Tabs`/segmented-control usage against the sizes already established in Dashboard/Analytics/the flight modal rather than improvising a fresh one.
